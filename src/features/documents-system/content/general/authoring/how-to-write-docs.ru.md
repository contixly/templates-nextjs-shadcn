---
title: "Как писать документацию"
description: "Правила структуры, metadata и содержания публичных документов"
group: "Общее"
groupOrder: 700
parentItem: "Введение"
order: 20
status: "published"
hide: true
toc: true
author: "Молчанов Н."
version: "1.0.1"
editedAt: "2026-06-01"
---

# Как писать документацию

Документ должен описывать одно понятное поведение, модуль или пользовательский сценарий. Если страница начинает
смешивать несколько независимых тем, её нужно разделить на дочерние документы.

## Metadata

Каждая страница начинается с metadata:

```md
---
title: "Название страницы"
description: "Короткое описание для заголовка и SEO"
group: "Events Program"
parentItem: "Voting Panel"
order: 10
status: "draft"
toc: true
---
```

`group` отвечает за верхний раздел в левом меню. `parentItem` группирует дочерние страницы внутри раздела. `order`
задаёт порядок страниц. `status: "draft"` и `status: "review"` скрыты в production, а `hide: true` скрывает страницу
в production при любом статусе.

`editedAt` указывайте вручную в формате `YYYY-MM-DD` для страниц, которые будут опубликованы или архивированы.

## Рекомендуемая структура страницы

Используйте устойчивый порядок блоков:

1. Краткое описание сценария или модуля.
2. Когда и для кого эта страница полезна.
3. Основные понятия и роли.
4. Пошаговый сценарий или правила поведения.
5. Ограничения, ошибки и edge cases.
6. Связанные страницы или компоненты.

## Заголовки и якоря

Правый список якорей строится по заголовкам второго уровня. Поэтому основные разделы страницы должны начинаться с `##`.
Заголовки третьего уровня используйте только внутри длинных разделов.

## MDX-компоненты

На старте достаточно обычного Markdown. Если нужен интерактивный или визуально выделенный блок, страницу можно перевести
в MDX и использовать общие компоненты:

- `Callout` — важная заметка, предупреждение или ограничение.
- `Steps` и `Step` — пошаговая инструкция.
- `Files`, `Folder`, `File` — структура файлов или модулей.
- `Tabs` — переключение между описанием, примером и кодом.

## Связь с кодом

Если документ описывает конкретный модуль, указывайте реальные пути:

- route в `src/app`;
- feature в `src/features`;
- shared-компоненты в `src/components`;
- тесты в `test`.

Это снижает риск устаревания документации и помогает быстро проверить, где живёт описанное поведение.
